(()=>{const el=document.getElementById('chart');if(!el)return;const rows=JSON.parse(el.dataset.rows||'[]');if(!rows.length){el.textContent=el.dataset.none||'No results.';return;}const W=Math.max(900,rows.length*112),H=350,pad=46,base=275,bw=20;let s=`<svg viewBox="0 0 ${W} ${H}" role="img" aria-label="${escapeHtml(el.dataset.aria||'Performance comparison')}"><line class="axis" x1="${pad}" y1="${base}" x2="${W-12}" y2="${base}"/>`;rows.forEach((r,i)=>{const x=pad+i*106+8;[['ret','bar1',0,el.dataset.ret],['known','bar2',23,el.dataset.known],['abs','bar3',46,el.dataset.absent]].forEach(([k,c,dx,label])=>{const v=Math.max(0,Math.min(1,+r[k]||0)),h=v*210;s+=`<rect class="${c}" x="${x+dx}" y="${base-h}" width="${bw}" height="${h}" rx="3"><title>${escapeHtml(r.name)} ${escapeHtml(label||k)}: ${(100*v).toFixed(1)}%</title></rect>`;});s+=`<text class="barlabel" transform="translate(${x},${base+18}) rotate(35)">${escapeHtml(r.name)}</text>`;});s+=`</svg><p class="muted legend"><span>■ ${escapeHtml(el.dataset.ret||'Retention')}</span><span>■ ${escapeHtml(el.dataset.known||'Known transfer')}</span><span>■ ${escapeHtml(el.dataset.absent||'Absent factor')}</span></p>`;el.innerHTML=s;function escapeHtml(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}})();
